/**
 * 
 */

let a ="This is a simple line"
let b ="This is another line.But there must be full stop in between them."

let c = `${a}.${b}`
console.log(c)

let someTextWithCurlyBraces = `this is some code with curly braces.\${Here is the text}`
console.log(someTextWithCurlyBraces)